Example environment variables you may want to store locally or in your n8n host env.

NOTE: n8n stores credentials in its own credential store. Use these env values only if you plan to use them when creating credentials.

OpenAI (example if you create credentials using an API key method)

OPENAI_API_KEY=REPLACE_YOUR_OPENAI_API_KEY_HERE

Google OAuth (if you create a custom Google OAuth app instead of using n8n's built-in setup)

GOOGLE_CLIENT_ID=REPLACE_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_GOOGLE_CLIENT_SECRET
GOOGLE_REDIRECT_URI=REPLACE_GOOGLE_REDIRECT_URI

Google Sheets

GOOGLE_SHEETS_SPREADSHEET_ID=REPLACE_GOOGLE_SHEET_ID

(Optional) If you want to version-control credential IDs that you manually insert into the sanitized JSON

REPLACE_GOOGLE_SHEETS_CREDENTIAL_ID=REPLACE_GOOGLE_SHEETS_CREDENTIAL_ID
REPLACE_GOOGLE_SHEETS_CREDENTIAL_NAME=REPLACE_GOOGLE_SHEETS_CREDENTIAL_NAME
REPLACE_OPENAI_CREDENTIAL_ID=REPLACE_OPENAI_CREDENTIAL_ID
REPLACE_OPENAI_CREDENTIAL_NAME=REPLACE_OPENAI_CREDENTIAL_NAME

(Optional) Any other API tokens you use with this workflow

OTHER_API_KEY=REPLACE_OTHER_API_KEY
